数据可视化综合知识点

一、Python 数据可视化生态系统概览

    1.1 主要可视化库对比分析
        Python 拥有丰富的数据可视化生态系统，每个库都有其独特的设计理念和适用场景。
            Matplotlib：基础绘图的基石
                Matplotlib 是 Python 数据可视化的基础库。它采用面向对象的设计理念，提供了极其灵活的绘图接口，几乎可以创建任何类型的静态图表。
                Matplotlib 的最大优势在于其高度可定制性，用户可以精确控制图表的每个元素，这使得它特别适合需要出版质量图表的科研场景。
                Matplotlib 也存在一些局限性。它的 API 相对复杂，即使创建简单图表也需要编写较多代码。默认的图表样式相对传统，缺乏现代感。
                对于大规模数据集（100,000 + 数据点），Matplotlib 的渲染速度会明显下降。
            Seaborn：统计可视化的优雅解决方案
                Seaborn 构建在 Matplotlib 之上，专注于统计数据的可视化。
                它提供了更高级的接口和更美观的默认样式，特别适合探索性数据分析（EDA）。
                Seaborn 的核心优势包括：与 Pandas DataFrame 的完美集成，可以直接使用数据框的列名创建复杂统计图表；
                内置了许多统计模型的可视化功能，如回归分析、相关性热力图等；提供了丰富的调色板和主题选项，使图表更加美观专业。
                Seaborn 的学习曲线相对平缓，适合快速创建美观的统计图表。
                但它也有局限性：由于基于 Matplotlib，某些高级定制仍需使用 Matplotlib 函数；不支持交互式图表，生成的都是静态图像。
            Plotly：交互式可视化的领导者
                Plotly 是一个现代化的交互式可视化库，支持多种编程语言。
                它的最大特点是原生支持交互功能，用户可以通过鼠标进行缩放、平移、悬停查看数据点详情等操作。
                Plotly 支持 3D 图表、地图可视化和动画效果，特别适合制作仪表板和 Web 应用。
                Plotly Express 作为 Plotly 的高级接口，提供了简洁一致的 API，类似 Seaborn 但产生交互式输出。
                大多数图表只需要一次函数调用即可创建，大大简化了使用流程。
                Plotly 生成的图表可以导出为 HTML 格式，非常适合在网页中展示。
                但需要注意的是，对于超大规模数据集，Plotly 在浏览器中的渲染速度可能会受到影响。
            Bokeh：高性能 Web 可视化
                Bokeh 是一个用于创建交互式 Web 图表的 Python 库。
                它专注于高性能的 Web 可视化，特别擅长处理大型数据集（百万级数据点）。
                Bokeh 提供了丰富的图表类型和交互功能，可以创建出高度可定制的交互式图表。
                Bokeh 的独特优势在于其流式数据处理能力和对大型数据集的高效渲染。它支持 WebSocket 实时更新，可以创建真正的实时仪表板。
                Bokeh 的图表可以直接在 Web 浏览器中显示，支持与用户的深度交互。但 Bokeh 的学习曲线较陡峭，需要更多的配置工作。
            Altair：声明式统计可视化
                Altair 是一个基于 Vega-Lite 规范的声明式统计可视化库。
                它的设计理念是让用户专注于 "做什么" 而不是 "怎么做"，通过简洁的语法实现复杂的可视化。
                Altair 的核心特点包括：基于 "图形语法" 的声明式语法，代码简洁优雅；
                        内置了强大的统计变换功能，可以直接在图表规范中进行数据处理；默认样式美观现代，符合现代设计美学。
                Altair 的优势在于其语法的简洁性和统计功能的强大性。它特别适合快速探索数据和创建统计图表。
                Altair 也有一些限制：基于 JavaScript 渲染，对大数据集（超过 100,000 点）可能存在性能问题；
                某些高级定制需要深入了解 Vega-Lite 规范。

    1.2 各库适用场景与学习难度
        各库的适用场景, 实际用到了自己查嘛，Matplotlib 和 Seaborn 基本就可以处理大部分场景。
            已有的 Python 和 Matplotlib、Seaborn基础，各库的学习难度排序如下：
                - Plotly Express（容易）：API 设计简洁一致，类似 Seaborn 但支持交互
                - Altair（中等）：声明式语法需要适应，需要理解数据类型系统
                - Plotly Graph Objects（中等偏难）：功能强大但 API 复杂，需要较多学习
                - Bokeh（较难）：概念较多，需要理解模型 - 视图架构，配置相对复杂
                - Matplotlib（最难精通）：虽然基础概念容易，但高级定制需要深入学习


二、核心可视化库高级特性详解

    简单了解新的库，有个印象即可，都学完，影响后面进度。

    2.1 Plotly 深度解析
        Plotly 作为现代化的交互式可视化库，在数据探索和展示方面具有独特优势。

        Plotly Express：简洁的高级接口
        Plotly Express 是 Plotly 的高级接口，设计理念是让用户用最少的代码创建美观的交互式图表。
        它的 API 设计简洁一致，类似 Seaborn 但产生交互式输出。
        基本使用非常简单。导入 Plotly Express 后，只需一行代码就能创建基础图表：
            base_plotly.py
            直接在 Web 中展示图，很，新颖

        Plotly Express 支持多种图表类型，包括：
            - 基础图表：散点图、折线图、柱状图、饼图、面积图、气泡图
            - 统计图表：直方图、箱线图、小提琴图、分布图、误差线图
            - 科学图表：热力图、等高线图、三元图、图像显示

        交互式功能详解
            Plotly 的交互式功能是其最大亮点之一。默认情况下，所有 Plotly 图表都支持以下交互操作：
                - 鼠标操作：
                    左键拖拽：旋转 3D 图表或平移 2D 图表
                    鼠标滚轮：缩放图表
                    右键拖拽：框选放大特定区域
                - 悬停功能：鼠标悬停在数据点上时，会显示该点的详细信息
                - 工具栏功能：图表右上角的工具栏提供了丰富的交互选项：
                    放大镜：放大指定区域
                    缩小镜：缩小图表
                    重置视图：回到初始视图
                    下载：导出图表为图片或数据
                    笔刷选择：选择多个数据点
                    框选缩放：框选后放大显示

        3D 图表的强大功能
            Plotly 在 3D 可视化方面表现出色，支持多种 3D 图表类型：
            3D 散点图展示三维空间中的数据分布：
                plotly_scatter_3D.py
                3D 鸢尾花，网页展示，可动

                plotly_graoh_objects.py
                3D 表面图，厉害
            3D 图表支持完整的相机控制，可以通过scene_camera参数设置视角

        Dash 集成：构建交互式 Web 应用
            Dash 是 Plotly 开发的 Web 应用框架，允许用户使用纯 Python 创建交互式 Web 应用。这为数据可视化提供了全新的可能性。

            一个简单的 Dash 应用示例：
                plotly_dash.py

                dash.Dash(__name__)：这是创建 Dash 应用的实例。它相当于给的网页应用搭建了一个最基础的骨架。

                px.data.gapminder()：这里直接调用了 Plotly Express (px) 内置的一个经典数据集（全球各国的人口、预期寿命等数据）。
                .query("country=='Canada'") 则是像写 SQL 一样，从这个庞大的数据表中精准筛选出了“加拿大”的数据行，并赋值给 df（DataFrame）。
                补充：
                    .query() 是 Python 数据分析神器 Pandas（DataFrame）中一个非常强大且优雅的数据筛选函数。
                    .query() 允许直接传入一个字符串形式的条件表达式，来过滤 DataFrame 中的行。
                    它的语法非常接近自然语言或者数据库里的 SQL 语句，读起来特别顺畅。
                    "country=='Canada'" 的意思就是：找出 country（国家）这一列的值完全等于 'Canada'（加拿大）的所有记录。

                    与传统方法相比：
                        # 传统写法（实现同样的筛选，通常需要使用“布尔索引”，比较啰嗦，符号多容易眼花）
                        # 先加载一次数据
                        full_df = px.data.gapminder()
                        # 再对已经加载好的数据进行筛选
                        df = full_df[full_df['country'] == 'Canada']

                        # .query() 写法（极其简洁、直观）
                        df = px.data.gapminder().query("country == 'Canada'")
                    px.data.gapminder() 的作用是从 Plotly 的内置库里加载一份完整的全球数据集。

                    还可以使用复杂的逻辑：
                        # 使用 & 表示“并且”，| 表示“或者”
                        filtered_df = df.query("year > 2000 & lifeExp > 80")
                        # !=、not in 和 not 都可以表示非
                        df.query("country != 'Canada'")
                        df.query("country not in ['Canada', 'United States']")
                        df.query("not country == 'Canada'")

                app.layout 决定了网页长什么样，它是由一个个组件像搭积木一样嵌套组成的：
                    - html.Div([...])：这是一个通用的容器，相当于 HTML 里的 <div> 标签，用来包裹页面上的其他元素。
                    - html.H1(...)：在页面上渲染出一个一级标题，显示文字“Canada Life Expectancy Trend”。
                    - dcc.Graph(...)：这是 Dash 专门用来承载 Plotly 交互式图表的核心组件。
                        - id='life-expectancy-chart'：给这个图表起了一个唯一的身份证号，
                                如果以后想通过按钮或滑块来控制这个图表，就需要通过这个 ID 来找到它。
                        - figure=px.line(...)：直接把刚才准备好的加拿大数据 df，用 Plotly Express 画成了一张折线图（x轴是年份，y轴是预期寿命），
                                然后塞进了 dcc.Graph 里进行展示。

                启动服务器
                if __name__ == '__main__':：这是 Python 的标准写法，确保只有当直接运行这个脚本时，下面的代码才会执行。
                app.run(debug=True)：它会启动一个本地服务器，默认端口通常是 8050。
                debug=True 开启了调试模式，它的最大好处是热重载：
                        当修改了代码并保存后，网页会自动刷新更新，不需要手动重启程序，非常适合开发阶段使用。


            Dash 支持丰富的组件库，包括按钮、滑块、下拉菜单等，使可以创建复杂的交互式仪表板。
            最新版本的 Dash 3.3.0 还集成了 Plotly Cloud，提供了更强大的开发工具。


    2.2 Bokeh 高级应用
        Bokeh 专注于高性能的 Web 可视化，特别适合处理大型数据集和需要实时更新的场景。

        1. Web 应用集成能力
            Bokeh 的一个重要特性是其强大的 Web 集成能力。它可以将可视化嵌入到现有的 Web 应用中，支持多种部署方式：

            - 简单嵌入：使用server_document函数可以轻松将 Bokeh 应用嵌入到网页中
                from bokeh.embed import server_document
                script = server_document("https://demo.bokeh.org/sliders")
            - Django 集成：通过bokeh-django库，可以将 Bokeh 无缝集成到 Django 项目中
                这个配置好麻烦，我现在不用这个，就不硬改了
            - 独立服务器部署：Bokeh 可以作为独立的服务器应用运行，支持通过 SSH 隧道访问受限主机。

        2. 处理大规模数据集的策略
            Bokeh 在处理大规模数据集方面表现出色，提供了多种优化策略：

            - WebGL 支持：使用ScatterGL代替Scatter可以显著提升大数据集的性能：
                base_ScatterGL.py

                LinearColorMapper（线性颜色映射器）：它就像一个“翻译官”。
                    把数值 z（比如 0 到 100）交给它，它会按照指定的调色板（代码中用的 Viridis256 一种从紫到黄的科学渐变色），
                    把每个具体的数字精准地翻译成对应的 RGB 颜色。
                    low 和 high 告诉它翻译的数值范围。
                ColumnDataSource（列式数据源）：这是 Bokeh 官方强烈推荐的“标准数据容器”。
                    它把原本独立的 NumPy 数组 x, y, z 打包成一个字典格式。
                    这样做不仅能让 Bokeh 在处理大量数据时跑得更快，还能让你在后面的绘图代码中直接用 'x', 'y', 'z' 这样的字符串来引用数据列。

                'x', 'y', source=source：这里不再直接传 NumPy 数组，而是去 source 这个数据容器里，找名为 'x' 和 'y' 的两列数据作为横纵坐标。
                color={'field': 'z', 'transform': color_mapper}：
                    - 'field': 'z' —— 找准“原材料”
                        含义：在数据容器（ColumnDataSource）里，找到名为 'z' 的那一列数据。
                        作用：它指定了用哪个变量来决定颜色。
                                Bokeh 会提取出这一列里的每一个数值（比如第1个点的 z=10，第2个点的 z=85...），作为后续计算颜色的基础原材料。
                    - 'transform': color_mapper —— 交给“翻译官”
                        含义：把刚才找到的 'z' 的数值，交给对象 color_mapper 去处理。
                        作用：这里的 color_mapper 就是前面定义的 LinearColorMapper。
                                它是一个严格的规则制定者，负责把抽象的数字翻译成具体的 RGB 颜色。
                            - 比如它内部的规则是：数值 0 对应紫色，数值 50 对应绿色，数值 100 对应黄色。
                            - 当 Bokeh 拿到某个点 z=75 时，就会通过这个 transform 自动算出它应该显示为“黄绿色”。

                render_mode='webgl'：scatter 方法不支持这个参数，删掉后图表就能正常渲染了。

                ColorBar：如果没有这根颜色条，看图表的人只能看到五颜六色的点，却不知道什么颜色代表大数值、什么颜色代表小数值。
                    ColorBar 就是把这个“翻译规则”以图例的形式展示出来。
                    - label_standoff=12：刻度数字（标签）与颜色条本体之间的水平间距 12 个像素的距离。
                    - location=(0, 0)：它决定了颜色条的锚点（起点）在哪里。

                p.add_layout(color_bar, 'right')：把这根颜色条像挂件一样，添加到画布 (p) 的右侧。
                    颜色条整体放在了画布的右侧边栏里。此时，location=(0, 0) 指的就是这个右侧边栏的左下角作为起点。

                注意：
                    render_mode='webgl'
                    Bokeh 中，只有 line（折线图）、patch（区块图）等少数几个图形支持 'webgl' 渲染模式，而 scatter 散点图是不支持的。
                    虽然去掉了 WebGL 加速，Bokeh 默认的 HTML5 Canvas 渲染引擎在处理 10 万个数据点时依然非常流畅，完全能够满足需求。
                    如果你未来确实需要用 'webgl' 来绘制超大规模的图表（比如百万级以上），建议改用 p.line() 或者 p.patch() 等方法，它们才支持开启 WebGL 硬件加速。
                    对实例代码散点图需求，直接使用默认配置就是最佳选择！

            - 数据分块处理：对于超大规模数据，可以采用分块加载的策略：
                bokeh_server_flow.py
                分段流式动态加载数据，还有动画效果

            - 使用 Datashader 进行大数据可视化：Bokeh 与 Datashader 集成，可以处理千万级数据点：

        3. 自定义交互功能
            Bokeh 提供了丰富的交互工具和事件处理机制，允许创建高度定制的交互效果：
            基本交互工具：
                PanTool：平移工具
                WheelZoomTool：滚轮缩放
                BoxZoomTool：框选缩放
                TapTool：点击选择
                LassoSelectTool：套索选择
                bokeh_data_shader.py

            - 自定义 JavaScript 回调：可以为图表元素添加 JavaScript 回调函数：
                JS_callback.py
                ColumnDataSource：这是 Bokeh 的核心数据容器。
                        它类似于一个字典或表格，专门用来存放绘图所需的数据（这里是 x 和 y 坐标）。Bokeh 的交互功能高度依赖它。
                figure()：创建一个基础的绘图区域（画布），后续所有的图形元素都会添加到这里。

                js_on_event('tap', callback)：给画布 p 绑定了一个“轻触/点击（tap）”事件。
                        当在图表上点击时，就会触发上面定义的 callback 回调函数。我试了，可以用。

            - 滑块和小部件集成：Bokeh 提供了丰富的小部件库，可以创建交互式仪表板：
                - 关于 doc 参数
                    因为 Bokeh 服务器（Bokeh Server）要求：所有「带 Python 交互」的图表，必须由一个函数创建，
                    并且这个函数必须接收一个参数：doc（文档对象），它是页面与 Python 后台之间的桥梁。
                    doc = Bokeh Document：它是整个页面的根，所有图表、滑块、按钮、回调必须挂在它上面；它负责：
                        - 把 Python 代码 → 变成网页
                        - 把浏览器操作 → 传回 Python 执行

                    slider.on_change('value', update_plot)
                        只有在 modify_doc 内部，才能使用 Python 回调！
                        这个回调必须运行在 Bokeh 服务环境里，必须连接到 doc，外面写 → 无法工作 → 报警告。

                    doc.add_root(layout)：把做好的整个布局，挂到文档上，让服务器能显示。

                    静态 HTML（不用函数，不用 doc）：show(layout)
                    Bokeh 服务（必须用函数 + doc）：
                        def modify_doc(doc):
                            # 代码
                            doc.add_root(layout)


    2.3 Altair 声明式语法精要
        Altair 采用独特的声明式语法，让用户专注于 “做什么” 而不是 “怎么做”，这种设计理念大大简化了复杂图表的创建过程。

            - 声明式语法的核心概念：
                Altair 的声明式语法基于 “图形语法”（Grammar of Graphics）的理念。
                在 Altair 中，创建图表的过程是声明性的：只需要告诉 Altair 想要展示什么，而不需要详细描述如何绘制。

                基本的 Altair 图表创建遵循以下模式：
                    base_altair.py

                这个简单的例子展示了 Altair 的核心概念：
                    - alt.Chart(data)：指定数据源
                    - .mark_circle()：指定标记类型（圆形）
                    - .encode()：指定数据到视觉属性的映射

            - 统计变换的强大功能
                Altair 内置了强大的统计变换功能，可以直接在图表规范中进行数据处理，无需在 Python 中预先计算。
                    altair_statistical_transformation.py

                    - 数据分箱（Binning）是常用的统计变换之一：
                        alt.Chart(cars).mark_bar().encode(...)
                            使用 cars 汽车数据集, 画 柱状图, 定义 X、Y 轴
                        alt.X('Horsepower').bin()
                            把马力从低到高分成一段一段（比如 50-100，100-150...）,就是直方图
                        y='count()'
                            统计每个马力区间里有多少辆车, 对应柱子的高度
                        补充：
                            alt.X() 作用
                                是的，专门声明 X 轴字段与轴配置，配套还有alt.Y()，用来精细定义坐标轴映射、格式、分箱等属性。
                            .bin() 分组规则
                                无参bin()：Altair 自动根据数据范围、数据量自适应划分区间组数
                                可手动指定：
                                    bin(maxbins=N) 限定最大分组数
                                    bin(step=N) 固定每个区间跨度
                            加 bin 和不加 bin 的区别：
                                不加 bin：以单个唯一马力值为横坐标，一个数值对应一根柱子，数值离散无合并
                                加 bin：把连续数值划分为区间段，同区间数据合并统计，一根柱子代表一个数值区间

                    - 聚合操作（Aggregation）支持多种统计函数：
                        支持的聚合函数包括：count()、sum()、mean()、median()、min()、max()等。
                        y='mean(Miles_per_Gallon)'
                            指定计算平均值，自动根据 X 轴分组后再计算平均值

                    - 分组聚合（Grouped Aggregation）可以按多个维度分组：
                        示例代码中改部分的操作和上一步的操作都是分组聚合

                    - 数据变换（Transformations）提供了更复杂的数据处理能力：
                        1. 计算变换（transform_calculate）：创建新的计算字段
                            data.population()：从 Altair 内置的 Vega_datasets 中加载美国人口统计数据。
                            x 和 y：绘制一个基础的柱状图，展示不同年份的人口总和。
                            alt.condition()：条件 这是 Altair 实现“如果...那么...”逻辑的核心方法。
                            alt.datum.sex == 1：alt.datum 是一个特殊的变量，代表数据集中的每一行。这里在判断当前数据的 sex 字段是否等于 1。

                            transform_calculate：这是 Altair 强大的数据预处理工具。
                                    它允许使用类似 JavaScript/Excel 的表达式，在不改变底层 Pandas DataFrame 的前提下，动态生成一个新的虚拟字段。
                            虽然在这段特定的代码中，颜色已经通过上面的 alt.condition 解决了，但 transform_calculate 创建的 sex_label 字段通常用于后续的其他用途，
                                    比如作为图例（Legend）的标签、工具提示（Tooltip）的文字，或者用于更复杂的分组筛选。

                            补充：
                                x='year:O'
                                    - year（字段名）：表示将数据表中的 year（年份）这一列，映射到图表的 X轴 上。
                                    - :O（数据类型后缀）：这里的 O 代表 Ordinal（有序分类数据）。
                                    虽然年份看起来像数字，但在这个柱状图中，并不把年份当作一个可以无限拉伸的连续数值，而是把它看作一个个独立的、有先后顺序的“分类标签”
                                            （比如2000年、2001年...）。
                                    视觉效果：标记为 :O 后，Altair 会在 X 轴上为每一个年份单独留出一个位置来画柱子，而不是把它们当成连续的坐标轴刻度。

                                y='sum(people):Q'
                                    - sum(people)（聚合计算）：这体现了 Altair 强大的自动计算能力。
                                            它表示在画图前，先对 people（人口数量）这一列的数据进行求和（sum）运算。
                                            不需要提前用 Pandas 的 groupby().sum() 去处理数据，Altair 会在后台帮你自动完成这个分组求和的过程。
                                    - :Q（数据类型后缀）：这里的 Q 代表 Quantitative（定量/连续数值数据）。
                                        因为人口数量是一个实实在在的数值，有大小之分，可以进行加减运算。
                                        视觉效果：标记为 :Q 后，Altair 会把 Y 轴渲染成一个连续的数值坐标轴，柱子的高度就严格对应求和后的人口数值大小。

                                Altair 常见的四种数据类型后缀:
                                    后缀	英文全称	        中文含义	    常见例子
                                    :Q	Quantitative	定量/数值	价格、温度、人口数量 (price:Q)
                                    :N	Nominal	        名义/无序分类	颜色、国家、性别 (country:N)
                                    :O	Ordinal	        有序分类	    评分等级（高/中/低）、月份、年份 (month:O)
                                    :T	Temporal	    时间/日期	具体的日期时间戳 (date:T)

                                alt.datum：数据的“替身”
                                    在 Altair 的声明式语法里，我们并没有像在 Pandas 中那样直接操作一个具体的 DataFrame 变量。
                                    alt.datum 是一个特殊的引用对象，它代表了传入图表中的每一行数据。
                                    可以把它想象成在告诉 Altair：“请你帮我把数据里的每一行都拿出来看一看”。
                                    Altair 会在后台遍历所有数据，当它发现某一行的 sex 等于 1 时，就应用第一个颜色（蓝色）；
                                            如果发现不等于 1（即等于 0），就应用第二个颜色（红色）。
                                补充：
                                    transform_calculate 内部使用的是 Vega 表达式字符串（类似 JavaScript 的语法）。
                                    在那段字符串里，不需要加 alt. 前缀，直接使用 datum 就能代表当前的数据行。
                                    两者本质上的作用是异曲同工的，都是为了对数据进行逐行的条件提取和计算！
                                    Python 环境：必须用 alt.datum：
                                        alt.datum 是 Altair 库里的一个特殊对象，Python 解释器需要通过 alt. 这个前缀来找到它。
                                        如果只写 datum.sex，Python 会报错说找不到 datum 这个变量。
                                    字符串表达式环境：直接用 datum：
                                        transform_calculate 接收的参数是一个字符串（用引号包起来的文本）。
                                        Altair 不会把这个字符串当作 Python 代码去执行，而是把它原封不动地打包，发送给浏览器端的可视化引擎（Vega-Lite）去解析。
                                        字符串内部，使用的是类似 JavaScript 的 Vega 表达式语法。
                                        在这个语法体系里，代表“当前数据行”的关键字天生就叫 datum，不需要任何 Python 库的前缀。

                                    关于被注释的代码：
                                        在 Altair（以及底层的 Vega-Lite）中，聚合运算（sum）的优先级高于条件判断（alt.condition）。
                                        先聚合，后判断，默认兜底
                                        聚合后原本的性别 sex 字段在求和过程中丢失了（或者变成了无法直接等于 1 的状态），
                                        Altair 就会统一执行 else 的逻辑，也就是你设置的第二个颜色——红色。所以所有的柱子都变成了红色。
                                        解决：
                                            color='sex_label:N'
                                            当把 sex_label（或 sex）字段映射给 color 参数时，实际上是向 Altair 下达了一个“分组指令”。
                                            只要把一个分类字段交给 color、xOffset 或者 column，就等同于告诉图表：“请按这个字段把数据拆开画！”
                                            会自动分配颜色和图例。

                        2. 过滤变换（transform_filter）：筛选数据
                            看代码语法很清晰

                        3. 聚合变换（transform_aggregate）：创建聚合后的数据
                            使用 transform_aggregate 时，它是一个非常严格的指令：必须明确指定输出的新字段名。
                            如果只写 groupby=['year'] 而不告诉它聚合后的数据叫什么名字，Altair 就无法生成有效的绘图数据，导致画布上什么都没有。
                            year:O（Ordinal，有序分类）。在 Altair 中，如果 X 轴是分类数据 (:O)，mark_line() 往往不知道该按什么顺序去连接这些离散的点，
                                    或者会认为它们之间没有连续性，从而导致不显示线条。
                            将 X 轴改为 year:T（Temporal，时间类型），Altair 就能完美识别出年份的连续递进关系，顺利画出连贯的折线。


            美观的默认样式与主题

                Altair 的另一个重要特性是其美观的默认样式和灵活的主题系统。
                内置主题提供了多种预设样式：
                    - default：默认主题
                    - opaque：不透明背景主题
                    - dark：深色主题
                    - light：浅色主题
                可以通过以下方式切换主题：
                    - alt.themes.enable('dark')  # 启用深色主题
                    - alt.themes.disable()       # 禁用主题，使用默认样式

                自定义主题允许创建个性化的视觉风格:
                    altair_themes.py


            交互功能与响应式设计

                Altair 虽然主要用于静态图表，但也提供了一些交互功能，并且具有良好的响应式设计(152)。
                简单交互包括：
                    - 鼠标悬停显示数据详情
                    - 点击选择数据点
                    - 缩放和平移（通过修饰符键）

                响应式图表：
                    altair_interactive.py
                    点击选中、悬停、缩放、拖动都有


三、图表类型选择策略与最佳实践

    3.1 常见图表类型详解
        掌握各种图表类型的特点和适用场景，是创建有效数据可视化的基础。以下是 Python 可视化库中支持的主要图表类型及其应用场景。

        - 折线图（Line Chart）
            折线图是最基本也是最常用的图表类型之一，主要用于展示数据随时间或有序类别变化的趋势。
            适用场景：
                - 时间序列数据的趋势分析
                - 展示连续数据的变化模式
                - 多组数据的对比分析

            matplotlib_line.py

        - 柱状图（Bar Chart）
            柱状图用于比较不同类别之间的数据大小，有垂直和水平两种形式。
            适用场景：
                - 分类数据的比较
                - 展示数据的分布情况
                - 多维度数据的对比分析

            seaborn_bar.py
                Set3 配色是好看的
                "whitegrid" 风格默认只会绘制与坐标轴刻度对齐的辅助线，而不会把整个图表画成密密麻麻的方格纸。
                对于柱状图来说，纵向的网格线通常是不必要的（柱子的位置已经由 X 轴的类别分开了），所以 Seaborn 默认只保留了对读数最有帮助的横向线。

        - 饼图（Pie Chart）
            饼图用于展示数据的比例分布，显示各部分占总体的百分比。
            适用场景：
                - 展示部分与整体的关系
                - 比较各部分的相对大小
                - 数据类别不宜过多（建议不超过 6 个）

            plotly_pie.py
                Plotly 相比 Matplotlib 等静态绘图库最大的优势之一——开箱即用的交互体验！
                Plotly 的核心设计理念就是制作“交互式”图表。
                当使用 plotly.express（通常简写为 px）的高级接口绘图时，它会自动生成一套默认的悬停提示框（Hover Tooltip），
                        无需编写任何额外的代码。
                觉得默认显示的信息不够用：
                    1. 简单微调（使用 hover_data）
                        想增加或减少显示的列，或者改变数字的小数位数，可以直接在 px.pie() 里加参数
                    2. 深度定制（使用 hovertemplate）
                        完全重写悬停框的文案、加入 HTML 标签（比如换行 <br>、加粗 <b>），可以使用 update_traces 配合模板字符串，例：
                        hovertemplate="<b>水果: %{label}</b><br>金额: %{value} 元<extra></extra>"

        - 散点图（Scatter Plot）
            散点图用于展示两个变量之间的关系，是探索性数据分析的重要工具。
            适用场景：
                - 分析变量间的相关性
                - 检测异常值
                - 展示数据的分布模式

            altair_scatter.py
                很简单的实现
                Altair 中，当使用像 data.cars() 这样的内置示例数据时，它实际上是一个包含了多项汽车指标的 Pandas DataFrame（数据表）。
                除了在代码中用到的马力（Horsepower）、油耗（Miles_per_Gallon）和产地（Origin）之外，它还自带了车辆的重量、气缸数、加速度等详细信息。
                size='Weight_in_lbs' （重量（磅））是一种声明式的写法。
                只需要把数据集中现有的列名直接告诉 Altair，它就会自动提取这一列的数据，并将其映射到散点图中圆点的大小上
                ——数值越大的车，画出来的圆点就越大。

        - 直方图（Histogram）
            直方图用于展示数据的分布情况，显示数据在各个区间的频率或数量。
            适用场景：
                - 展示数据的分布形态
                - 检测数据的偏度和峰度
                - 发现数据中的异常模式

            bokeh_histogram.py
                np.histogram(...)：这是关键的一步。它并没有直接画图，而是先对数据进行“分桶”统计。
                bins=30：把这 1000 个数据切分成 30 个区间（柱子）。
                density=True：将结果转换为概率密度。
                    这意味着 Y 轴代表的是密度而不是单纯的频数（所有柱子的面积加起来等于 1）。
                    这一步返回了两个数组：hist（每个柱子的高度/密度）和 edges（每个柱子的左右边界坐标）。
                        left=edges[:-1]：取边界数组的前 N-1 个值作为每个矩形的左边缘。
                        right=edges[1:]：取边界数组的后 N-1 个值作为每个矩形的右边缘。
                        （edges[:-1] 和 edges[1:] 完美错开一位，刚好构成了 30 个连续的区间）。

        - 箱线图（Box Plot）
            箱线图用于展示数据的统计分布，包括中位数、四分位数、异常值等信息。
            适用场景：
                - 展示数据的分布特征
                - 比较多组数据的分布
                - 检测异常值

            seaborn_box.py
                没啥说的，一般把数据集写在最前面，语义清晰

        - 热力图（Heatmap）
            热力图使用颜色编码展示数据矩阵中的数值大小，特别适合展示相关性或密集数据。
            适用场景：
                - 展示相关性矩阵
                - 显示数据的密度分布
                - 可视化大型数据集的模式

            seaborn_heatmap.py
                np.random.randn(10, 5)：利用 NumPy 随机生成了一个包含 10 行（样本）和 5 列（特征/变量）的数据矩阵。
                np.corrcoef(...)：这是计算皮尔逊相关系数矩阵的核心函数。
                    在 NumPy 的 np.corrcoef() 函数中，默认情况下是将每一行（row）视为一个独立的变量，而将每一列视为观测值。
                    它会计算这 10 个变量两两之间的相关性，最终返回一个 10x10 的方阵。
                    矩阵对角线上的值永远是 1.0（代表变量与自身的完全正相关），其他位置的值在 -1 到 1 之间。
                sns.set_theme(style="white")：将 Seaborn 的全局主题设置为纯白背景（不带网格线）。
                    这种干净的背景非常适合展示热力图，能让色块的颜色更加突出。
                vmin=-1, vmax=1：强制将颜色条的范围锁定在 -1 到 1 之间。
                    因为相关系数的理论取值范围就是 [-1, 1]，这样做能确保颜色的映射是准确且标准的。
                plt.setp(ax.get_xticklabels(), rotation=45, ha="right")：这是一个非常实用的排版技巧。
                    它将 X 轴的刻度标签（也就是那 5 个变量的名字）旋转 45 度，并将对齐方式设为右对齐（ha="right"）。
                    这样可以有效防止当变量名较长或变量较多时，X 轴的文字发生重叠，让图表看起来更整洁。


    3.2 图表选择决策树
        面对众多的图表类型，如何根据数据特征和分析目标做出正确选择。
        这个真的很有用：
            开始
            ├── 数据类型
            │   ├── 一维数据
            │   │   ├── 连续型 → 直方图、箱线图、密度图
            │   │   └── 离散型 → 柱状图、饼图
            │   ├── 二维数据
            │   │   ├── 两个连续变量 → 散点图、热力图
            │   │   ├── 一个连续一个分类 → 条形图、箱线图
            │   │   └── 两个分类变量 → 堆积柱状图、分组柱状图
            │   └── 多维数据
            │       ├── 时间序列 → 折线图、面积图
            │       └── 其他多维 → 散点图矩阵、平行坐标
            ├── 分析目标
            │   ├── 展示趋势 → 折线图、面积图
            │   ├── 比较大小 → 柱状图、条形图
            │   ├── 显示分布 → 直方图、箱线图
            │   ├── 展示关系 → 散点图、网络图
            │   └── 显示比例 → 饼图、圆环图
            ├── 数据量
            │   ├── 小数据量(<1000) → 所有类型
            │   ├── 中等数据量(1000-100000) → 大部分类型
            │   └── 大数据量(>100000) → 散点图(需降采样)、热力图、线图
            └── 交互需求
                ├── 需要交互 → Plotly、Bokeh
                └── 静态展示 → Matplotlib、Seaborn、Altair


    3.3 可视化最佳实践
        掌握了图表类型的选择，还需要了解一些通用的最佳实践，以确保可视化的有效性和专业性。

        - 颜色使用原则
            颜色是可视化中最重要的视觉元素之一，正确使用颜色可以大大提升图表的可读性：
                1. 避免过多颜色：同一图表中颜色种类不宜超过 5 种
                2. 使用色盲友好的调色板：避免使用红绿色盲难以区分的颜色组合
                3. 颜色要有意义：颜色应与数据含义相关，如红色表示高值，蓝色表示低值
                4. 保持一致性：相同含义的数据应使用相同的颜色

            matplotlib_color.py

                生成图例那部分重点注意一下

                plt.style.use('seaborn-v0_8-darkgrid')：直接调用了 Matplotlib 内置的 Seaborn 暗色网格风格。
                    这会让图表自带专业的灰色背景和白色网格线。
                plt.rcParams['axes.prop_cycle'] = ...：这是非常进阶的一步。
                    它修改了 Matplotlib 的“属性循环器”，意味着以后你在同一张图上连续画图时，
                    线条或柱子会严格按照指定的这组十六进制颜色（经典的 Tableau 配色方案）自动循环上色，保证了整套图表配色的统一性。
                使用了面向对象的绘图方式 fig, ax = plt.subplots()，比直接用 plt.bar() 更灵活。
                mpatches.Patch(...)：相当于在内存里凭空“画”出了几个带颜色的小方块（补丁）。
                    代码用列表推导式生成了 5 个分别对应上述颜色的小方块，并命名为 "Color 1" 到 "Color 5"。
                    普通的柱状图没有像折线图那样通过 label= 参数自动生成图例，所以这里引入了 matplotlib.patches 模块。
                ax.legend(handles=legend_elements, ...)：
                    把这些手工制作的“小方块”作为图例元素（handles），放置在图表的右上角（loc='upper right'）。

        - 字体与标签规范
            清晰的标签和适当的字体大小是图表可读性的关键：
                1. 标题：使用 14-16pt 粗体字，概括图表的主要内容
                2. 轴标签：使用 12pt 字，清晰描述轴的含义
                3. 刻度标签：使用 10pt 字，确保数值清晰可读
                4. 图例：使用 10-11pt 字，位置应不遮挡主要数据

        - 数据标注技巧
            在图表中添加适当的数据标注可以增强信息传达效果：
                1. 峰值标注：标注数据的最大值和最小值
                2. 趋势线标注：标注重要的趋势或转折点
                3. 数值标签：在柱状图、饼图等图表上直接显示数值
                4. 误差线：显示数据的不确定性或误差范围

            plotly_marker.py
                y = np.sin(x) * np.exp(-0.1 * x)
                    带衰减的正弦曲线
                    sin(x)：上下波动; exp(-0.1x)：让幅度越来越小（慢慢衰减）(单调递减)
                fig = go.Figure()
                    创建一张空白的 Plotly 图
                fig.add_trace(go.Scatter(x=x, y=y, mode='lines', name='Signal'))
                    添加一条折线
                    mode='lines'：用线连接
                    name='Signal'：图例名字叫 Signal
                peak_index = np.argmax(y)
                    自动找到 y 值最大的那个位置的序号

        避免常见误区
            了解并避免以下常见误区，可以显著提升可视化的质量：
                1. 不要使用 3D 效果装饰：3D 图表往往会扭曲数据，除非真的需要展示三维信息
                2. 避免使用过多特效：阴影、渐变等特效会分散注意力
                3. 保持简单：图表应简洁明了，不要试图在一个图表中展示所有信息
                4. 正确使用饼图：饼图只适合展示比例，且类别不宜过多


四、科研与商业应用场景实战

    4.1 科研绘图规范与技巧
        科研绘图有其特殊的规范要求，需要在美观性和科学性之间找到平衡。以下是科研绘图的核心规范和实用技巧。

            期刊图表规范要求
            不同期刊对图表有不同的要求，但通常包括以下几个方面：
                - 分辨率要求：
                    线图：至少 1200 DPI
                    位图：至少 300 DPI
                    组合图：至少 600 DPI
                - 字体大小规范：
                    标题：10-12pt
                    轴标签：8-10pt
                    刻度标签：6-8pt
                    图例：6-8pt
                - 颜色模式：
                    印刷：CMYK 模式
                    屏幕展示：RGB 模式
                - 文件格式：
                    矢量图：EPS、PDF
                    位图：TIFF、JPEG

            Matplotlib 科研图表优化
                使用 Matplotlib 创建符合科研规范的图表需要一些特殊设置：

                    matplotlib_chart_style.py
                        plt.rcParams['text.usetex'] = True
                            设置会让 Matplotlib 调用系统里的 LaTeX 来渲染文字，但Windows 一般默认不装 LaTeX
                            99% 的绘图根本不用 LaTeX;装 LaTeX 要下载 4GB+ 安装包;直接关闭 text.usetex 即可解决
