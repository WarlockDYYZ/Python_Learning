[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my_data_tool"
version = "0.1.0"
authors = [
  { name="Lxf", email="test@qq.com" }
]
description = "preprocessing + feature_engineering"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.8"
dependencies = [
  "pandas>=1.0",
  "scipy>=1.0",
  "openpyxl"
]