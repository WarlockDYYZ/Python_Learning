Streamlit

第一部分：Streamlit 基础入门

    1.1 Streamlit 核心理论
        Streamlit 是一个专为数据科学家和机器学习工程师设计的 Python Web 应用框架。它的核心理念是：用纯 Python 脚本快速构建交互式数据应用，无需前端知识。

        核心优势（理论）
            1. 零前端门槛：不需要 HTML/CSS/JS，全部用 Python 函数调用生成 UI 组件
            2. 脚本式运行：每次用户交互都会重新运行整个脚本，数据流自上而下单向流动，心智模型极其简单
            3. 声明式 API：写"我要显示什么"而非"怎么渲染"，例如 st.write(df) 直接展示 DataFrame
            4. 热重载：修改代码后浏览器自动刷新，开发效率极高
            5. 原生支持数据科学栈：无缝集成 Pandas、Matplotlib、Plotly、PyTorch 等主流库

        适用场景
            - 数据探索与可视化看板（Dashboard）
            - 机器学习模型演示与调参工具
            - 交互式数据分析报告
            - 内部数据工具快速原型

        运行原理（重渲染机制）
            核心机制：脚本全量重跑。
                    每当用户与组件交互（如拖动滑块、点击按钮），Streamlit 会从头到尾重新执行整个 Python 脚本，组件的当前值作为变量注入，
                    最后将新结果渲染到页面。
            这意味着：普通变量在每次交互后都会重置。
                    需要跨交互保留状态 → 使用 st.session_state；需要避免重复计算 → 使用缓存装饰器。

        第一个 Streamlit 应用：
            app.py

            集成了其他的三方库，语法上我还是都看得懂

            运行应用：
                streamlit run app.py

        常用运行参数
            参数	                说明	                示例
            --server.port	    指定端口	            streamlit run app.py --server.port 8080
            --server.headless	不自动打开浏览器	    streamlit run app.py --server.headless true
            --theme.base	    主题（light/dark）	streamlit run app.py --theme.base dark


    1.2 页面基础元素
        表格数据展示；JSON 与代码展示；媒体元素；数据下载
            table_show.py

            见名知意啊，重要


    1.3 数据与媒体展示
        文本与标题类；分割线与进度；

            data_and_media.py

        st.write 万能函数（重点），Streamlit 的瑞士军刀，根据传入参数类型自动选择渲染方式：
            - 字符串 → 渲染为 Markdown
            - DataFrame → 渲染为交互式表格
            - Matplotlib/Plotly 图 → 渲染为图表
            - 字典 → 渲染为 JSON 树
            - 多个参数 → 依次渲染


第二部分：核心组件与交互系统

    2.1 组件体系理论
        组件分类体系
        Streamlit 组件按功能分为四大类：
            类别	    作用	                        典型组件
            输入组件	接收用户输入，返回值给 Python	slider, selectbox, text_input, button
            展示组件	渲染数据、文本、图表	        dataframe, write, pyplot, plotly_chart
            布局组件	组织页面结构	                sidebar, columns, tabs, expander
            控制组件	控制执行流程	                stop, form, rerun

        交互原理（核心理论）
            Widget → 变量 → 重渲染 三步循环：
                1. 用户操作组件（拖动滑块）
                2. 组件值更新，触发脚本全量重跑
                3. 组件函数返回当前值赋给变量，页面重新渲染
                每个输入组件都有一个返回值，直接赋值给变量即可使用：
                    age = st.slider("选择年龄", 0, 100, 25)
                    st.write(f"你的年龄是：{age}")

        组件状态持久化
            默认情况下，组件的值由 Streamlit 内部维护，每次重跑时从组件状态中读取并返回。
            但普通 Python 变量会被重置。
            如果需要跨交互保存数据（如计数器、用户登录态），必须使用 st.session_state。

        表单（Form）机制
            默认每个组件交互都会立即触发重渲染。
            当需要多个输入填完后一起提交时，使用 st.form() 将组件分组，只有点击提交按钮才触发重跑。


    2.2 输入组件大全
        数值型输入；选择型输入；文本型输入；日期与时间；按钮与触发类
            stools.py

    2.3 布局与容器组件
        侧边栏（Sidebar）
            侧边栏是数据分析看板最常用的布局，筛选器统一放在左侧，主区域展示结果。
        列布局（Columns）
            将页面横向分为多列，常用于并排展示指标卡或图表。
        选项卡（Tabs）
            同一区域切换不同视图，适合多维度分析。
        折叠面板（Expander）
            默认收起，点击展开，适合放置高级筛选或辅助信息。
        容器（Container）与占位符
            layout_and_container.py


    2.4 会话状态管理（session_state）
        核心问题：Streamlit 每次交互都会重跑整个脚本，普通 Python 变量会被重新初始化。例如计数器，每次点击按钮都会重置为 0。
        st.session_state 是一个跨脚本重跑持久化的字典，数据保存在用户会话中，刷新页面才会重置。

        基本用法；与组件双向绑定；回调函数（on_change / on_click）；
            session_state.py

        典型应用场景
            - 多步骤表单：保存当前步骤和已填写数据
            - 筛选条件记忆：切换 Tab 时保留用户选择
            - 用户登录态：记录是否登录、用户信息
            - 缓存中间计算结果：避免重复计算


    2.5 缓存机制原理
        缓存的必要性
        由于 Streamlit 每次交互都重跑脚本，如果数据加载、模型预测等耗时操作每次都执行，页面会非常卡顿。缓存机制让相同输入只计算一次，后续直接返回结果。

        两大缓存装饰器：
            装饰器	            缓存对象	                    适用场景
            @st.cache_data	    数据（DataFrame、数值、字符串）	数据加载、数据处理、计算函数
            @st.cache_resource	资源对象（连接、模型）	        数据库连接、ML 模型、大文件句柄

            cache_data.py

            - 关于“刷新数据”
                点击按钮后数据确实会被删除，但以为 Streamlit 的特性，点击后运行到 st.rerun() 时，脚本会从头执行一次，再次加载数据。
                所以显示网页上数据并没有在点击 “刷新数据” 时消失。

        缓存参数详解
            参数	            说明	                        默认值
            ttl	            缓存存活时间（秒），过期自动失效	None（永久）
            max_entries	    最大缓存条目数，超出后淘汰最旧的	None（不限制）
            show_spinner	计算时是否显示加载提示	        True
            persist	        是否持久化到磁盘（重启不丢失）	False

        缓存原理（理论）
            缓存命中逻辑：函数名 + 参数值 → 计算哈希 → 查找缓存 → 命中则返回，未命中则执行函数并存入缓存。
            注意事项：
                - 参数必须是可哈希的（列表、字典不能直接作为参数，需转为 tuple / frozenset）
                - 函数内部修改全局变量会导致缓存结果不准确
                - mutable 返回值会被自动深拷贝，防止外部修改污染缓存
            补充：
                字典类型，键、字典中的元素(item)，都是可哈希的，字典本身不是可以哈希的。


第三部分：可视化集成

    3.1 Matplotlib / Seaborn 集成
        集成原理
            Streamlit 通过 st.pyplot() 渲染 Matplotlib 图表。核心是获取 matplotlib 的 Figure 对象传给 Streamlit。

        Matplotlib 基础集成：
            st_matplotlib.py

        Seaborn 统计图表集成：
            st_seaborn.py

        交互式筛选联动图表：
            select_chart.py

        注意：
            - 显式创建 Figure：推荐用 fig, ax = plt.subplots() 而不是 plt.plot() 全局方式，避免重跑时图表叠加
            - 及时关闭：渲染后调用 plt.close(fig) 释放内存，大数据量看板尤其重要
            - 中文字体：部署环境需安装中文字体，否则中文显示为方框


    3.2 Plotly 交互式可视化
        为什么选 Plotly
            Plotly 生成的是交互式 HTML 图表，支持鼠标悬停查看数值、缩放、平移、下载图片，比 Matplotlib 静态图更适合数据分析看板。
            Streamlit 原生支持 st.plotly_chart()。

        基础折线图：
            base_line.py

        常用图表类型：
            usual_chart.py

        交互式筛选联动：
            interactive_chart.py


    3.3 Streamlit 原生图表
        原生图表简介
            Streamlit 内置了一组基于 Vega-Lite 的轻量图表 API，无需导入额外库，适合快速展示数据。
            优点是写法极简，缺点是自定义程度不如 Plotly。

            base_chart.py

            为什么这里绘图时不用设置中文显示呐？


第四部分：实战项目 — 电商销售分析面板

    4.1 项目需求与设计
        项目背景
        某电商平台运营团队需要一个交互式销售分析面板，用于日常监控销售表现、多维度下钻分析、快速生成数据结论。
        替代传统的静态 Excel 报表，实现数据实时交互探索。

        - 核心功能需求
            1. KPI 指标卡：总销售额、订单数、客单价、同比增长率
            2. 多维度筛选：日期范围、产品品类、销售渠道、地区
            3. 趋势分析：日/周/月销售趋势折线图
            4. 结构分析：品类占比饼图、渠道分布柱状图
            5. 地区分析：各省份销售额排行 + 地图可视化
            6. TOP 商品：热销/滞销商品排行榜
            7. 数据导出：筛选结果一键导出 CSV

        - 页面布局设计

        - 技术选型
            - UI 框架：Streamlit（侧边栏 + 列布局 + Tabs）
            - 图表库：Plotly（交互式，悬停查看详情）
            - 数据处理：Pandas + NumPy
            - 缓存策略：@st.cache_data 缓存数据加载
            - 状态管理：session_state 保存筛选条件


    4.2 数据层设计
        数据模型设计
            模拟电商销售明细表，包含以下字段：
                字段名	        类型	    说明
                order_id	    字符串	订单唯一编号
                order_date	    日期	    下单日期
                category	    字符串	产品品类（电子/服装/食品/家居/美妆）
                product_name	字符串	商品名称
                channel	        字符串	销售渠道（天猫/京东/抖音/拼多多/线下）
                region	        字符串	省份地区
                quantity	    整数	    购买数量
                unit_price	    浮点数	单价
                sales_amount	浮点数	销售额 = 数量 × 单价
                customer_type	字符串	客户类型（新客/老客）


    4.3 完整实现代码
        sales_dashboard.py

        运行效果
            1. 左侧筛选栏：日期、品类、渠道、地区四维筛选，修改后图表自动更新
            2. 顶部 KPI 卡：四个核心指标一目了然
            3. 趋势图：双 Y 轴柱状+折线组合，支持日/周切换
            4. 中部两列：品类环形图 + 渠道横向柱状图
            5. 下部两列：地区排行 + TOP10 热销商品
            6. 底部数据明细：支持展开查看和 CSV 导出


    4.4 功能模块拆解
        模块一：页面配置层
            st.set_page_config(
                page_title="电商销售分析面板",  # 浏览器标签页标题
                page_icon="📊",                 # 标签页图标
                layout="wide",                  # 宽屏模式（默认 centered）
                initial_sidebar_state="expanded"  # 侧边栏默认展开
            )
            数据分析看板推荐 layout="wide"，充分利用屏幕宽度展示多列图表。

        模块二：数据层（缓存设计）
            使用 @st.cache_data(ttl=3600) 装饰数据加载函数：
                - 首次运行生成数据，后续 1 小时内直接返回缓存
                - 避免每次交互都重新生成 5000 条数据，提升响应速度
                - 生产环境替换为读取数据库 / CSV / API

        模块三：筛选交互层
            侧边栏统一放置所有筛选器，形成「左侧筛选 + 右侧结果」的经典看板布局：
                筛选维度	组件类型	    设计原因
                日期范围	date_input	时间是最常用的筛选维度
                产品品类	multiselect	支持多选对比分析
                销售渠道	multiselect	多渠道组合分析
                销售地区	multiselect	区域维度下钻
            交互机制：任一筛选条件变化 → 脚本重跑 → filter_data() 重新过滤 → 所有图表基于新数据重绘。

        模块四：KPI 指标卡层
            使用 st.columns(4) 四列等宽布局，配合 st.metric() 组件：
                - 第一屏就能看到核心结论，符合管理层「先看总数再看明细」的阅读习惯
                - delta 参数展示同比/环比变化，红绿箭头自动标识涨跌
                - 生产环境中 delta 值应通过对比上一周期计算得出

        模块五：图表可视化层
            采用「趋势 + 结构 + 排行」三层分析结构：
                1. 趋势层：时间维度折线/柱状图，看整体走势和波动
                2. 结构层：饼图 + 横向柱状图，分析品类和渠道构成
                3. 排行层：地区排行 + 商品 TOP10，定位重点区域和爆品
            全部使用 Plotly，支持悬停查看具体数值、缩放聚焦、一键下载图片

        模块六：数据导出层
            csv_data = df_filtered.to_csv(index=False).encode("utf-8-sig")
            st.download_button(
                label="📥 导出筛选结果 CSV",
                data=csv_data,
                file_name=f"销售数据_{date_range[0]}_{date_range[1]}.csv",
                mime="text/csv"
            )
            使用 utf-8-sig 编码，确保 Excel 打开中文不乱码。

        优化建议
            1. 数据缓存：原始数据加载加 @st.cache_data，只加载一次
            2. 增量过滤：筛选在 Pandas 内存中完成，避免重复 IO
            3. 图表数据预聚合：绘图前先 groupby 聚合，不要传明细数据给图表库
            4. 大数据量分页：数据明细表只展示前 N 条，全量数据走导出
            5. 组件状态绑定：复杂筛选条件存入 session_state，支持 Tab 切换不丢失

进阶练习
    - 接入真实数据库：替换 generate_mock_data 为 SQL 查询
    - 增加用户登录：st.experimental_user 或自定义鉴权
    - 多页面应用：使用 Streamlit Pages 拆分为总览、商品、用户等多个子页面
    - 定时刷新：st_autorefresh 组件实现实时大屏
    - 下载 PDF 报告：结合 pdfkit 生成静态报告